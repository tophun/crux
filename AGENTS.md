# 저장소 작업 규칙

## PR Checker CI

- Pull Request를 생성하거나 업데이트한 뒤에는 PR Checker의 모든 필수 체크가 완료될 때까지 상태를 확인합니다.
- `gh pr checks <PR 번호>`로 체크 결과를 확인하고, 실패한 GitHub Actions는 `gh run view <run-id> --log`로 원인을 확인합니다.
- 체크가 실패하면 실패 원인을 요약한 뒤 가장 작은 범위로 수정하고, 관련 로컬 검증을 실행한 다음 커밋·푸시합니다.
- 수정 후에는 같은 PR의 PR Checker를 다시 확인합니다. 모든 필수 체크가 성공할 때까지 실패한 체크의 로그 분석과 수정을 반복합니다.
- 외부 서비스 체크는 직접 수정하지 않고 상세 링크와 상태만 보고합니다.
- 최종 보고에는 PR URL, 마지막 검사 시각의 체크별 결과, 적용한 수정과 로컬 검증 결과를 포함합니다. 실행하지 않은 검증을 성공했다고 보고하지 않습니다.
